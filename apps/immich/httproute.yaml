apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: immich
  namespace: immich
spec:
  parentRefs:
    - name: traefik-gateway
      namespace: default
  hostnames:
    - "photos.koutoulastha.dev"
  rules:
    - backendRefs:
        - name: immich-server
          port: 2283
