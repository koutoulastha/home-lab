# piraeus snapshot-controller chart bundles CRDs + controller
installCRDs: true
controller:
  replicaCount: 1
  resources:
    requests:
      cpu: 25m
      memory: 32Mi
    limits:
      memory: 64Mi
